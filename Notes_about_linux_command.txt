
What is zombie in linux Command by giving the top command ?
	
	If Parent ID is killed before the Child ID , Then Child ID Becomes the zombie



su 

	Used to switch to root user (So that super user permission can be execute commands)


su username 
	
	Used to switch to a different user


sudo 

	Executes only that cammand with root / super user privileges



What is Shell ?

	The Shell is a command line Interpreter . It translates Commands entered by the user and converts them into a language that is understand by the kernel


