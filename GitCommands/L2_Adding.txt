1) 	This is clone project 

*)  clone the project 
	git clone https://github.com/project.git (https commands)

*)	Adding file into Staging Area
	git add filename 
	(OR)
	git add *

*)	Adding file into commit Area
	git commit -m 'git_commit'

*)	Adding file into Central Repository
	git push origin main

2)  Why we need origin ?
	When we clone a remote project then origin is referring to that
	but when we start developing on local, then we need to set the origin 


2) Creating a project in local 

*) 	initilize the git 
	git init 
	git status 

*) How to set the origin ?
	git remote add origin https://github.com/bigdatabysumit-trendytenc/lendingproj.git (HTTPS link)

*) How to check the origin ?
	git remote -v 

*) Check the current branch ?
	git branch 

*) How to rename the branch ?
	git branch -M main 

	Here we rename the old branch (master) to new name branch (main) 

	git push origin main 

	How to upstrem the command ? If i don't want use the origin main in the git push and need to 
	
	git push -u origin main  

	git push 


3) Existing Project 